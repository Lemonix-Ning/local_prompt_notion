# Context Index (启动上下文)

> **用途**: 最小充分上下文 - AI 的唯一启动入口

---

## 1. Current Focus

### Current Objective
- 维护现有功能稳定性
- 完成 Lumi 交互与反馈统一（气泡/嘴巴/点击思考）
- 完成倒计时前端逻辑简化与验证

### Todo List
#### 🔥 高优先级
- [ ] UI 走查：Lumi 点击思考、气泡优先级、睡眠唤醒
- [ ] 倒计时刷新重置验证（interval 任务）
- [ ] 任务类型验证（interval/one-time/daily/weekly/monthly）

---

## 2. Map (High-Level Only)

```
lumina/
├── src/                    <-- 前端 (React + TypeScript)
│   ├── components/         <-- UI 组件
│   ├── contexts/           <-- React Context 提供者
│   ├── hooks/              <-- 自定义 Hooks
│   ├── utils/              <-- 工具函数
│   ├── adapters/           <-- 文件系统适配器
│   └── api/                <-- HTTP 客户端
├── server/                 <-- 后端 (Node.js + Express)
│   ├── routes/             <-- API 路由
│   └── utils/              <-- 后端工具
├── src-tauri/              <-- 桌面应用 (Tauri + Rust)
├── vault/                  <-- 数据存储 (文件系统)
└── scripts/                <-- 构建脚本
```

---

## 3. Architecture Summary

- **技术栈**: React 18.2 + TypeScript 5.3 + Tauri 2.9 + Node.js Express
- **状态管理**: Context + Reducer (禁止直接修改 state，必须通过 dispatch)
- **数据持久化**: 文件系统 (vault/ 目录，每个 Prompt = meta.json + prompt.md)
- **适配器模式**: 组件禁止直接调用 adapter，必须通过 AppContext 封装方法
- **性能约束**: 启动时间 < 2s，大列表使用虚拟滚动，Markdown 使用缓存

---

## 4. Reference Pointers

- **定位文件/模块** → 查 `PROJECT_MAP.md`
- **架构/模式调整** → 查 `ARCHITECTURE.md`
- **当前上下文/进度** → 查 `CURRENT_STATE.md`
- **协作规则/规范** → 查 `AI_RULES.md`
- **Bug 修复历史** → 查 `BUGFIX_LOG.md`

---

**最后更新**: 2026-01-24 | **Token 预算**: ~400 Tokens
